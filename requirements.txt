# Core
aiohttp>=3.10.0
httpx>=0.27.0
uvloop>=0.19.0
aiodns>=3.0.0
aiofiles>=24.1.0
pyyaml>=6.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Circuit breaker & caching
aiobreaker>=0.6.0
cachetools>=5.5.0

# Monitoring
prometheus-client>=0.20.0

# Optional for speed
orjson>=3.10.0
re2>=1.0.0

# Sing-box (not a Python package, but we use it as binary)
# The binary will be downloaded and cached in CI
